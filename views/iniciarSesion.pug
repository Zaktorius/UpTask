<!DOCTYPE html>
html(lang="en")
    head
        include partials/headers
    body.login
        .contenedor-formulario
            h1 Iniciar sesión en UpTask
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") Email
                    input(type="text" name="email" placeholder="Ingresa tu mail..." value= email)
                .campo
                    label(for="password") Password
                    input(type="password" name="password" placeholder="Ingresa tu password..." value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesion")
                .campo
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? Crea una gratis 